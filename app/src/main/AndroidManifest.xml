<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jose.birdwatchingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!-- android:networkSecurityConfig="@xml/network_security_config" -->
        <activity
            android:name=".View.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.SignupActivity"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".View.SightingsActivity" />
        <activity android:name=".View.MySightingsActivity" />
        <activity android:name=".View.ChallengesActivity" />
        <activity android:name=".View.NewSightingActivity" />
        <activity android:name=".View.AchievementsActivity" />
        <activity android:name=".View.BirdActivity" />
        <activity
            android:name=".View.SightingActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".View.BirdsActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".View.BirdsMenuActivity"></activity>
    </application>

</manifest>